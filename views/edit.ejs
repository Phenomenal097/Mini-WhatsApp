<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit chat</title>
</head>
<body>
    <h3>Edit chat for user:<%=chatDetails._id%></h3>
    <p>Chat shared to <b><%= chatDetails.to %></b> from <b><%= chatDetails.from %></b></p>
    <form action="/chats/<%=chatDetails._id%>?_method=PUT" method="POST">
        <textarea name="msg" rows="20" cols="50"><%=chatDetails.msg%></textarea>
        <button>Edit Message</button>
    </form>
</body>
</html>